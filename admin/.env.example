# Presuya — Credenciales admin (NUNCA commitear .env real)
# Duplicá este archivo como ".env" y pegá las credenciales.

# URL de tu proyecto Supabase (misma que usa el frontend)
SUPABASE_URL=https://bziztkhunfvwetnlczbj.supabase.co

# SERVICE ROLE KEY — se saca de:
# Supabase Dashboard → Project Settings → API → service_role (secret)
# ATENCIÓN: este key NO puede ir al frontend ni al repo.
SUPABASE_SERVICE_ROLE_KEY=pega-aca-tu-service-role-key
